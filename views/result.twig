<!DOCTYPE html>

<html>
    <head>
        <title>Your Car Dealership's Results</title>
    </head>
    <body>
        <h1>Your Car Dealership</h1>
        <ul>
            {% for car in cars %}
                <li>{{car.getMake_model}}</li>
                <li>{{car.getPrice}}</li>
                <li>{{car.getMiles}}</li>
                <img src="{{car.getImg}}">
            {% endfor %}
        </ul>
        <a href="/">Add another car</a>
        <a href="/delete">Delete all posts</a>
    </body>
</html>
